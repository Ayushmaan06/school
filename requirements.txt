# Render deploy only: the read-only web UI (school_intel.api.app), NOT the
# full pipeline. `enrich`/`extract`/CLI need the rest of pyproject.toml's
# dependencies (OCR, PDF parsing, httpx, typer...) - that stuff only runs
# locally / on the worker box, never imported by the web process, and
# rapidocr-onnxruntime>=1.3 has no Linux wheel on PyPI, which is what broke
# the last two deploys. Regenerate with `uv sync --all-groups` locally; this
# file is intentionally hand-scoped, not `uv pip compile` output.
fastapi==0.141.1
uvicorn==0.52.4
jinja2==3.1.6
sqlalchemy==2.0.52
psycopg[binary]==3.3.5
pydantic-settings==2.15.0
python-multipart==0.0.32
pyyaml==6.0.3
